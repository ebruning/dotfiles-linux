#
# wm independent hotkeys
#

## EGB - My Specific settings
# terminal emulator
super + Return
	alacritty

# program launcher
super + @space
	rofi -modi drun -show drun # 	albert show removing albert until fixing focus

super + f
    	thunar

super + l
	betterlockscreen -l dimblur

super + m
	bspc desktop -l next

super + q
	bspc node -{c,k}

super + r
	rofi -modi drun -show drun 

super + x
     	~/.local/bin/powermenu.sh

super + Escape
	pkill -USR1 -x sxhkd

# windows
super + {Left,Down,Up,Right}
	dir={west,south,north,east}; \
	bspc node -s "$dir.local" --follow \
	    || bspc node -m "$dir" --follow

super + alt + {Left,Down,Up,Right}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

## EGB End
